#ifndef _COMMON_H
#define _COMMON_H

#include <limits.h>

#define FIRSTITEMOFARRAY 1

enum ReturnTypeCommon { FAILURE = -1, SUCCESS };
enum Boolean { FALSE, TRUE };
enum ReturnTypeHeapExtractMax { FAILURE_HEM = INT_MIN };

#endif